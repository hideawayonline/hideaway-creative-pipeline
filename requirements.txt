requests>=2.31.0
google-auth>=2.28.0
google-api-python-client>=2.120.0
python-dotenv>=1.0.0
# Optional: HPSv2 human-preference scoring for best-of-N variant ranking.
# Heavy (pulls torch; downloads model checkpoint on first use) — the pipeline
# runs without it and simply skips scoring if it is not installed.
hpsv2>=1.2.0
